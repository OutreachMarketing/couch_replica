require 'spec_helper'

describe CouchReplica do
  it 'has a version number' do
    expect(CouchReplica::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
